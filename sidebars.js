// @ts-check
// ↑ 開啟 TypeScript 檢查（即使你用的是 JS）
// 好處：
// - 拼字錯誤會警告
// - type 寫錯會提醒
// - 對新手非常友善（強烈建議保留）

/**
 * 這一行是在告訴編輯器與 Docusaurus：
 * 下面這個 sidebars 物件，必須符合官方定義的結構
 *
 * SidebarsConfig 是「規則書」
 * 你寫的內容必須照這個規則來
 */
const sidebars = {
  /**
   * mainSidebar
   * 這個名字是你自己取的（不是固定）
   *
   * 👉 它代表「一整條 sidebar」
   * 👉 之後在 docusaurus.config.js 會指定要用哪一條
   *
   * 可以有：
   * - mainSidebar
   * - wikiSidebar
   * - resolveSidebar
   * 多條都可以
   */
  mainSidebar: [
    // =========================
    // 1️⃣ 首頁（第一個顯示）
    // =========================
    {
      // type: 'doc'
      // 👉 代表「單一文件」
      // 👉 sidebar 裡的一個 item
      type: 'doc',

      // id: 'home'
      // 👉 對應 docs/home.md
      // 👉 實際規則是「docs/ 底下的相對路徑（不含 .md）」
      id: 'home',

      // label
      // 👉 sidebar 顯示的文字
      // 👉 不寫的話，會用 md 檔裡的 title
      label: '🏠 首頁',
    },

    // =========================
    // 2️⃣ DaVinci Resolve 分類
    // =========================
    {
      // type: 'category'
      // 👉 代表「資料夾 / 分類」
      // 👉 裡面可以再包 doc / category
      type: 'category',

      // label
      // 👉 這個分類在 sidebar 顯示的名稱
      label: '🎬 DaVinci Resolve',

      // collapsed: false
      // 👉 預設是否收起
      // false = 一進網站就展開
      // true  = 預設收起
      collapsed: false,

      // items
      // 👉 這個分類底下有哪些東西
      items: [
        {
          // type: 'autogenerated'
          // 👉 讓 Docusaurus 自動掃描資料夾
          // 👉 依照檔案結構幫你產 sidebar
          type: 'autogenerated',

          // dirName
          // 👉 對應 docs/resolve 資料夾
          // 👉 裡面的 md、子資料夾都會被納入
          dirName: 'resolve',
        },
      ],
    },

    // =========================
    // 3️⃣ 你自己的 Docusaurus 筆記
    // =========================
    {
      type: 'category',
      label: '🦕 Docusaurus 筆記',
      collapsed: true, // 筆記類通常預設收起比較乾淨
      items: [
        {
          type: 'autogenerated',
          dirName: 'Docusaurus', // docs/Docusaurus
        },
      ],
    },

    // ❌ Tutorial（官方文件）
    // 沒有寫在這裡
    // 👉 就不會出現在 sidebar
  ],
};

// 匯出 sidebars
// Docusaurus 會讀這個檔案來產生側邊欄
export default sidebars;